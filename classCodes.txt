// to open git: right-click on the folders where the changes will be made

git init // initialize a new repository
git status // check repository status
git add "mycode..." // add code on repository
git add . // add all archives from folder on the next commit
git commit -m "what was modifield" // modify branch with message inserted, add code version
git config --global user.email "email...@email.com" // add email used on git
git config --global user.name "author" // add the author of that version
git push // upload code changes on active cloud (copy code git push --set... and execute command)
git remote add origin https://github.com/repository // indicates repository of versões on github
git reflog // check code versions
git reset --hard num versionnumbercode // change to desired version
git branch // see available branches and used branch
git branch branchsname // create new branch (used branch is greend)
git checkout branchname // switche to other branch banchname
git pull // upload all the lastest updates from the active branch

// push code from staring branch to master
git pull
git checkout master
git merge staging
git push

git checkout -b newBranchName masterBranch // Create a branch from the main branch

// git ignore
create the document
git status // the document name will be in red color
touch .gitignore // a text archive will be created in the folder '.gitignore'
open .gitignore, copy a document name, write on text archive and save
git status // to confirm
git add .
git status
git commit -m "add a gitignore archive"
git push
